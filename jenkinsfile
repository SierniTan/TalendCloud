pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                // bat 'python --version'
                echoe "Running ${env.BUILD_ID} and prev: ${currentBuild.previousSuccessfulBuild}"
            }
        }
    }
}
