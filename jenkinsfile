pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                // bat 'python --version'
                echo "Running ${env.BUILD_ID} and prev: ${env.previousSuccessfulBuild}"
            }
        }
    }
}
